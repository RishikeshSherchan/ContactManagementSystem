export const getContactController = async (req, res) => {
  res.status(200).json({ message: "All cocntact" });
};

export const setContactController = async (req, res) => {
  res.status(200).json({ message: "Set All cocntact" });
};
